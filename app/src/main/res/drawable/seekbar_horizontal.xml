<?xml version="1.0" encoding="UTF-8"?>
<layer-list
    xmlns:android="http://schemas.android.com/apk/res/android">
    <!--默认背景颜色或者图片-->
    <item android:id="@android:id/background" android:drawable="@color/light_gray" />
    <!--第二层进度颜色（缓存之类的）-->
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="2.0dip" />
                <gradient android:startColor="#80ffd300" android:endColor="#a0ffcb00" android:angle="270.0" android:centerY="0.75" android:centerColor="#80ffb600" />
            </shape>
        </clip>
    </item>
    <!--进度的颜色或者图片-->
    <item android:id="@android:id/progress">
        <clip android:drawable="@color/orange" />
    </item>
</layer-list>
